#include <iostream>
#include <string>
using namespace std;

bool iskind(string &s)
{
    if (s.length() == 0)
        return true;

    int len = s.length();
    for (int i = 0; i < len; i++) //탐색 시작점
    {
        for (int j = 1; j < len; j++)
        {
            
        }
    }
}

string make(string &s, int &n) //길이 n의 좋은 수열을 만들어야 함
{
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);

    int n;
    cin >> n;
}